# Decisions

## U0 package names

Checked by public web search on 2026-06-12:

- `9999css` — no search result found for an existing npm package.
- `realitycss` — no search result found for an existing npm package.

This is not a final npm reservation. Before U15 publish work, confirm directly against the npm registry and record the registry response.

## U0 scope

U0 is scaffold only. Do not build components, docs homepage screens, registry files, or package publishing until later U-series units.
